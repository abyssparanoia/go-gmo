name: CI

on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up Go 1.22
        uses: actions/setup-go@v6
        with:
          go-version: 1.25

      - name: test
        run: make test
